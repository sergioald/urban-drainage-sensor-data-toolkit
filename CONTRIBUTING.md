# Contributing

This repository is primarily a public-safe portfolio and research-software conversion. Contributions should preserve the confidentiality boundary: do not add real credentials, live endpoints, private client data, runtime logs, or generated operational files.

Before opening a pull request, run:

```bash
python -m pytest
python -m ruff check .
```
