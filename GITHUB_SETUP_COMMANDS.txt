# Suggested GitHub setup

# From the repository folder:
git init
git add .
git commit -m "Initial public-safe repository conversion"
git branch -M main

# Then create an empty GitHub repo named:
# urban-drainage-sensor-data-toolkit

git remote add origin https://github.com/sergioald/urban-drainage-sensor-data-toolkit.git
git push -u origin main

# Recommended repository description:
# Public-safe urban drainage telemetry QA/QC toolkit converted from legacy monitoring scripts.

# Recommended topics:
# python, hydrology, urban-drainage, sensor-data, time-series, qaqc, research-software, telemetry
