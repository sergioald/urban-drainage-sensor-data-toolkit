# Security policy

Do not report private credentials or sensitive operational details in public issues. If a secret is accidentally committed, rotate the credential immediately and remove it from the repository history before publication.
