# Optional dependencies needed by parts of the sanitized legacy script archive.
# The legacy scripts also depended on Windows paths, external services, and private credentials
# that are intentionally not included in this public repository.
matplotlib
scipy
scikit-learn
joblib
openpyxl
python-docx
folium
selenium
psycopg2-binary
PyYAML
progressbar2
wxPython; platform_system == "Windows"
pywin32; platform_system == "Windows"
